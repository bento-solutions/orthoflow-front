export const environment = {
  production: false,
  // The backend runs on 8080 (context path /api/v1). If something else on
  // your machine already owns 8080, start the backend on another port and
  // change this locally — but do not commit that change.
  apiUrl: 'http://localhost:8080'
};
